# Compiled collector configuration for the reporting-worker Agent — Service Tier tier-2.
#
# Generated by 'otel-guardrail compile-fleet'; do not edit this file, the next
# rollout overwrites it. Change one of its two inputs instead:
#
#   Telemetry Contract: contracts/reporting-worker.yaml — what this service emits
#   Pipeline Profile: tier-2-supporting — how that telemetry ships
receivers:
    otlp:
        protocols:
            grpc:
                endpoint: 0.0.0.0:4317
            http:
                endpoint: 0.0.0.0:4318
processors:
    batch:
        send_batch_size: 8192
        timeout: 15s
    memory_limiter:
        check_interval: 1s
        limit_mib: 256
    resource:
        attributes:
            - action: upsert
              key: deployment.environment
              value: production
            - action: upsert
              key: service.instance.id
              value: reporting-worker-b32f01
            - action: upsert
              key: service.name
              value: reporting-worker
            - action: upsert
              key: service.namespace
              value: analytics
            - action: upsert
              key: service.version
              value: 0.9.3
exporters:
    otlp/gateway:
        endpoint: otel-gateway.observability.svc.cluster.local:4317
        retry_on_failure:
            enabled: true
        sending_queue:
            enabled: true
            queue_size: 5000
service:
    pipelines:
        metrics:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
        traces:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
