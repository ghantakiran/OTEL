# Compiled collector configuration for the payments-api Agent — Service Tier tier-1.
#
# Generated by 'otel-guardrail compile-fleet'; do not edit this file, the next
# rollout overwrites it. Change one of its two inputs instead:
#
#   Telemetry Contract: contracts/payments-api.yaml — what this service emits
#   Pipeline Profile: tier-1-critical — how that telemetry ships
receivers:
    otlp:
        protocols:
            grpc:
                endpoint: 0.0.0.0:4317
            http:
                endpoint: 0.0.0.0:4318
processors:
    batch:
        send_batch_size: 8192
        timeout: 5s
    memory_limiter:
        check_interval: 1s
        limit_mib: 512
    resource:
        attributes:
            - action: upsert
              key: deployment.environment
              value: production
            - action: upsert
              key: service.instance.id
              value: payments-api-7d9f4b
            - action: upsert
              key: service.name
              value: payments-api
            - action: upsert
              key: service.namespace
              value: payments
            - action: upsert
              key: service.version
              value: 2.4.1
            - action: delete
              pattern: ^otel\.platform\.
exporters:
    otlp/gateway:
        endpoint: otel-gateway.observability.svc.cluster.local:4317
        retry_on_failure:
            enabled: true
        sending_queue:
            enabled: true
            queue_size: 10000
service:
    pipelines:
        logs:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
        metrics:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
        traces:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
    telemetry:
        resource:
            deployment.environment: production
            otel.platform.config_version: sha256:3b04a21de671883f1d9ad933ba65d71642e76cafbc88bb4e21a25a52f3a44b45
            service.instance.id: payments-api-7d9f4b
            service.name: payments-api
            service.namespace: payments
            service.version: 2.4.1
        metrics:
            level: normal
            readers:
                - periodic:
                    exporter:
                        otlp:
                            endpoint: https://otel-gateway.observability.svc.cluster.local:4317
                            protocol: grpc
                    interval: 30000
