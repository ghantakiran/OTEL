# Compiled collector configuration for the orders-api Agent — Service Tier tier-1.
#
# Generated by 'otel-guardrail compile-fleet'; do not edit this file, the next
# rollout overwrites it. Change one of its two inputs instead:
#
#   Telemetry Contract: contracts/orders-api.yaml — what this service emits
#   Pipeline Profile: tier-1-critical — how that telemetry ships
receivers:
    otlp:
        protocols:
            grpc:
                endpoint: 0.0.0.0:4317
            http:
                endpoint: 0.0.0.0:4318
processors:
    batch:
        send_batch_size: 8192
        timeout: 5s
    memory_limiter:
        check_interval: 1s
        limit_mib: 512
    resource:
        attributes:
            - action: upsert
              key: deployment.environment
              value: production
            - action: upsert
              key: service.instance.id
              value: orders-api-4c1e8a
            - action: upsert
              key: service.name
              value: orders-api
            - action: upsert
              key: service.namespace
              value: fulfilment
            - action: upsert
              key: service.version
              value: 5.1.0
            - action: delete
              pattern: ^otel\.platform\.
exporters:
    otlp/gateway:
        endpoint: otel-gateway.observability.svc.cluster.local:4317
        retry_on_failure:
            enabled: true
        sending_queue:
            enabled: true
            queue_size: 10000
service:
    pipelines:
        logs:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
        metrics:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
        traces:
            receivers:
                - otlp
            processors:
                - memory_limiter
                - resource
                - batch
            exporters:
                - otlp/gateway
    telemetry:
        resource:
            deployment.environment: production
            otel.platform.config_version: sha256:df1d11f63fd44dff750eb7bf20092037b6a05e6f1f1f0193198cedc870cf5c36
            service.instance.id: orders-api-4c1e8a
            service.name: orders-api
            service.namespace: fulfilment
            service.version: 5.1.0
        metrics:
            level: normal
            readers:
                - periodic:
                    exporter:
                        otlp:
                            endpoint: https://otel-gateway.observability.svc.cluster.local:4317
                            protocol: grpc
                    interval: 30000
